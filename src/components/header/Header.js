import { HeaderContainer } from "./HeaderStyles"

export const Header = () => {
    return(
        <HeaderContainer>
            <a href="#!" className="logo">LoveC<span>oo</span>k</a>
        </HeaderContainer>
    )
}